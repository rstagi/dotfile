#!/bin/zsh
set -u

print -r -- "claude $*" >> "${FAKE_ENGINE_LOG:?FAKE_ENGINE_LOG required}"
jq -cn '{outcome:"done"}' > "${FAKE_RUN_DIR:?FAKE_RUN_DIR required}/status.json"
print -r -- '{"type":"result","result":"done"}'
